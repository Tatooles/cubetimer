import type { TimerSettings } from "../sessions/types";

export const DEFAULT_SETTINGS: TimerSettings = {
  density: "comfortable",
  showGraph: true,
  showHistogram: true,
  showDraw: true,
  inspection: false,
};
